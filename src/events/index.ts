import * as ready from "./ready";
import * as interaction_create from './interaction.create';
import * as guild_member_add from './guild.member.add';

export const events = {
    ready,
    interaction_create,
    guild_member_add,
};
